<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_grey"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_temperature"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="left"
        android:paddingLeft="32dp"
        android:paddingTop="36dp"
        android:paddingBottom="36dp"
        android:text="--,- °C"
        android:textColor="@color/light_asphalt_blue"
        android:textSize="48sp"
        android:background="@android:color/white" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_below="@id/tv_temperature">

        <ListView
            android:id="@+id/forecast_list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        </ListView>

    </LinearLayout>

    <LinearLayout
        android:id="@+id/shadow"
        android:layout_width="match_parent"
        android:layout_height="12dp"
        android:orientation="horizontal"
        android:layout_below="@id/tv_temperature"
        android:background="@drawable/background_with_shadow" />

    <ImageView
        android:id="@+id/iv_weather"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="76dp"
        android:layout_alignRight="@+id/choose_city"
        android:layout_alignEnd="@+id/choose_city" />

    <com.melnykov.fab.FloatingActionButton
        android:id="@+id/choose_city"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="32dp"
        android:padding="30dp"
        android:src="@drawable/marker"
        fab:fab_colorNormal="@color/light_blue"
        fab:fab_colorPressed="@color/light_blue_dark"
        fab:fab_colorRipple="@color/light_asphalt_blue"/>

    <LinearLayout
        android:id="@+id/reload_weather_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="gone">
        <Button
            android:id="@+id/reload_weather_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="reloadWeather"
            android:text="Reload weather"/>
    </LinearLayout>

</RelativeLayout>
